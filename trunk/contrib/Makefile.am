SUBDIRS = osx
DIST_SUBDIRS = osx

confdir = $(sysconfdir)/tor

EXTRA_DIST = exitlist tor-tsocks.conf torify.1 tor-control.py tor.nsi tor.sh torctl

conf_DATA = tor-tsocks.conf

bin_SCRIPTS = torify

man_MANS = torify.1
